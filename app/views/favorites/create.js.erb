$("#js-favorites-create-button").replaceWith('<%= escape_javascript(link_to "お気に入りを解除する", favorites_path(profile_id: @profile.id),
method: :delete, class: "btn btn-primary favorite-button", remote: true, data: {profile_id: @profile.id}, id: "js-favorites-delete-button") %>');

